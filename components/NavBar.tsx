import {AppBar, Toolbar} from '@mui/material';

export default function NavBar() {
    return(
        <div>
            <AppBar>
                <Toolbar/>
            </AppBar>
        </div>
    )
}